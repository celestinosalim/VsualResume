import fakeData from "../fakeData.js";

const initialState = {
  resume: fakeData
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case value:

    default:
      return state;
  }
};

export { reducer };
